# API 명세

강쌤과외 수업 보고서 시스템의 로그인·보고서 작성 API 명세다.

## 공통

### 인증

- 로그인하면 JWT 토큰을 발급한다. 토큰에는 사용자 id와 role(`teacher` / `student`)이 들어 있다.
- 로그인 외의 요청은 헤더에 토큰을 붙인다.

```
Authorization: Bearer <token>
```

### 시간 형식

ISO 8601, 한국 시간 기준. 예: `2026-09-24T14:00:00+09:00`

### 에러 응답

```json
{ "message": "에러 설명" }
```

| 코드 | 의미 |
|---|---|
| 400 | 입력값 오류 |
| 401 | 로그인 실패, 토큰이 없거나 잘못됨 |
| 403 | 권한 없음 (역할이 다른 토큰, 내 수업이 아님) |
| 409 | 이미 보고서를 쓴 수업 |

## API 목록

| # | 메서드 | 경로 | 인증 | 설명 |
|---|---|---|---|---|
| 1 | POST | `/api/teachers/login` | 없음 | 선생님 로그인 |
| 2 | POST | `/api/students/login` | 없음 | 학생 로그인 |
| 3 | GET | `/api/teachers/me/lessons` | 선생님 | 보고서를 쓸 수업 목록 |
| 4 | POST | `/api/reports` | 선생님 | 보고서 등록 |

## 보고서 작성 화면 흐름

1. 선생님이 로그인하면(1번) 토큰과 이름을 저장한다.
2. 화면에 들어오면 작성 선생님 칸에 로그인한 이름을 보여준다. 수정할 수 없다.
3. 3번을 불러 수업 선택 목록을 채운다.
4. 수업을 고르면 3번 응답에 있던 그 수업의 값으로 채운다. 따로 호출하지 않는다.
   - 수업 종류: 수정할 수 없다. 화면 라벨은 '수업 종류'다.
   - 수업 시간: 주 선생님은 수정할 수 있고, 보조 선생님에게는 잠근다.
5. 보고서 내용과 숙제 내용을 쓰고 작성 완료를 누르면 4번으로 등록한다.

---

## 1. 선생님 로그인

`POST /api/teachers/login`

**요청**

```json
{
  "name": "김주",
  "phone": "01011110001"
}
```

| 필드 | 타입 | 필수 | 설명 |
|---|---|---|---|
| name | string | O | 이름 |
| phone | string | O | 전화번호. 하이픈이 섞여 오면 서버가 숫자만 남겨서 비교한다. |

**응답 `200`**

```json
{
  "token": "eyJhbGciOi...",
  "teacher": { "id": 1, "name": "김주" }
}
```

**에러**

- `401`: 이름과 전화번호가 맞는 선생님이 없음

## 2. 학생 로그인

`POST /api/students/login`

요청 필드는 1번과 같다.

**응답 `200`**

```json
{
  "token": "eyJhbGciOi...",
  "student": { "id": 1, "name": "홍길동" }
}
```

**에러**

- `401`: 이름과 전화번호가 맞는 학생이 없음

## 3. 보고서를 쓸 수업 목록

`GET /api/teachers/me/lessons` · 선생님 토큰

로그인한 선생님이 주 선생님이거나 보조 선생님인 수업을 `started_at` 기준 최신순으로 준다.

**응답 `200`**

```json
[
  {
    "lesson_id": 10,
    "student_name": "홍길동",
    "lesson_type": "regular",
    "started_at": "2026-09-24T14:00:00+09:00",
    "ended_at": "2026-09-24T16:00:00+09:00",
    "role": "main"
  }
]
```

| 필드 | 설명 |
|---|---|
| lesson_id | 수업 id |
| student_name | 학생 이름 |
| lesson_type | `regular`(정규) / `ontact`(온택트). 화면에선 수정할 수 없게 보여준다. |
| started_at, ended_at | 수업 시작·종료 시각 |
| role | `main`이면 수업 시간을 고칠 수 있고, `assistant`면 잠근다. |

수업 선택 목록의 표시 문구는 `started_at`과 `student_name`으로 만든다. 예: 9/24(수) 14:00 · 홍길동

**에러**

- `401`: 토큰이 없거나 잘못됨
- `403`: 선생님 토큰이 아님

## 4. 보고서 등록

`POST /api/reports` · 선생님 토큰

**요청**

```json
{
  "lesson_id": 10,
  "started_at": "2026-09-24T14:00:00+09:00",
  "ended_at": "2026-09-24T16:10:00+09:00",
  "report_content": "이차함수 최대·최소 단원을 마무리했습니다.",
  "homework_content": "교재 84~89쪽"
}
```

| 필드 | 타입 | 필수 | 설명 |
|---|---|---|---|
| lesson_id | number | O | 수업 id |
| started_at | string | O | 수업 시작 시각. 수업 날짜는 그대로 두고 시각만 바꿔서 보낸다. |
| ended_at | string | O | 수업 종료 시각 |
| report_content | string | O | 보고서 내용 |
| homework_content | string \| null | X | 숙제 내용. 숙제가 없으면 `null` |

- 작성 선생님은 토큰에서 가져온다.
- 수업 종류는 받지 않는다. 보고서에서는 수업 종류를 바꿀 수 없다.

**처리 순서**

1. 로그인한 선생님이 이 수업의 주 선생님이나 보조 선생님이 아니면 `403`.
2. 주 선생님이면 보낸 시각으로 lesson의 `started_at`, `ended_at`을 수정한다. 보조 선생님이 보낸 시각은 무시한다.
3. report를 저장한다. 이 선생님이 이미 이 수업에 보고서를 썼으면 `409`.

2와 3은 한 트랜잭션으로 묶는다. 3이 실패하면 2의 수정도 취소된다.

**응답 `201`**

```json
{ "report_id": 55 }
```

**에러**

- `400`: 보고서 내용이 비었거나, 주 선생님이 보낸 종료 시각이 시작 시각보다 빠르거나 같음
- `401`: 토큰이 없거나 잘못됨
- `403`: 선생님 토큰이 아니거나, 이 수업의 주 선생님·보조 선생님이 아님
- `409`: 이미 이 수업에 보고서를 씀
